#include "stdafx.h"

//find the index of the num which is in the list

/*
int main() {
	int numBuf[201];
	int n;
	while (scanf("%d", &n) != EOF) {
		for (int i = 0; i < n; i++) {
			scanf("%d", &numBuf[i]);
		}

		int requestNum, ans = -1;
		scanf("%d", &requestNum);
		for (int i = 0; i < n; i++) {
			if (requestNum == numBuf[i])
			{
				ans = i;
				break;
			}

		}
		printf("%d", ans);
	}
	return 0;
}*/